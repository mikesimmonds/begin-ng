import { WordCounterDirective } from './word-count-input.directive';

describe('WordCountInputDirective', () => {
  it('should create an instance', () => {
    const directive = new WordCounterDirective();
    expect(directive).toBeTruthy();
  });
});
